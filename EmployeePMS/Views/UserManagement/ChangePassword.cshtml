@model DL_CompetencyPMS.ViewModel.ChangePasswordVM

@{
    Layout = null;
    ViewBag.Title = "Change Password";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Change Password - Competency PMS</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        body {
            background: linear-gradient(to right, #d7e1ec, #f0f4f8);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .card-form {
            max-width: 460px;
            width: 100%;
            background-color: white;
            padding: 2rem;
            border-radius: 1rem;
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.08);
        }

        .form-title {
            font-weight: 600;
            margin-bottom: 1.5rem;
            text-align: center;
            color: #2c3e50;
        }

        .btn-sky {
            background-color: lightskyblue;
            color: white;
        }

            .btn-sky:hover {
                background-color: deepskyblue;
            }
    </style>
</head>
<body>

    <div class="card-form">
        <h3 class="form-title">Change Password</h3>

        @using (Html.BeginForm("ChangePassword", "UserManagement", FormMethod.Post))
        {
            @Html.AntiForgeryToken()

            <div class="mb-3">
                @Html.LabelFor(m => m.UserID, new { @class = "form-label" })
                @Html.TextBoxFor(m => m.UserID, new { @class = "form-control", placeholder = "Enter your User ID" })
                @Html.ValidationMessageFor(m => m.UserID, "", new { @class = "text-danger" })
            </div>

            <div class="mb-3">
                @Html.LabelFor(m => m.OldPassword, new { @class = "form-label" })
                @Html.PasswordFor(m => m.OldPassword, new { @class = "form-control", placeholder = "Current password" })
                @Html.ValidationMessageFor(m => m.OldPassword, "", new { @class = "text-danger" })
            </div>

            <div class="mb-3">
                @Html.LabelFor(m => m.NewPassword, new { @class = "form-label" })
                @Html.PasswordFor(m => m.NewPassword, new { @class = "form-control", placeholder = "New password" })
                @Html.ValidationMessageFor(m => m.NewPassword, "", new { @class = "text-danger" })
            </div>

            <div class="mb-3">
                @Html.LabelFor(m => m.RetypeNewPassword, new { @class = "form-label" })
                @Html.PasswordFor(m => m.RetypeNewPassword, new { @class = "form-control", placeholder = "Retype new password" })
                @Html.ValidationMessageFor(m => m.RetypeNewPassword, "", new { @class = "text-danger" })
            </div>

            <div class="d-grid">
                <button type="submit" class="btn btn-sky">Change Password</button>
            </div>
        }

        @if (ViewBag.ErrorMessage != null)
        {
            <div class="alert alert-danger mt-3 text-center">
                @ViewBag.ErrorMessage
            </div>
        }

        @if (ViewBag.SuccessMessage != null)
        {
            <div class="alert alert-success mt-3 text-center">
                @ViewBag.SuccessMessage
            </div>
        }
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
